/**
 * NinjaPear API
 * NinjaPear is a data platform that seeks to serve as the single source of truth for B2B data, be it to power your data-driven applications or your sales-driven workflow.  As a data client of NinjaPear API, you can: 1. Look up the customers, investors, and partners/platforms of any business globally. 2. (FREE) Retrieve the logo of any company. 3. (FREE) Find out the nature of an email address. 4. (FREE) Check your credit balance. 5. Monitor companies for updates (blog posts, X/Twitter posts, website changes) via RSS feeds.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: hello@nubela.co
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Target from './Target';

/**
 * The Feed model module.
 * @module model/Feed
 * @version 1.0.0
 */
class Feed {
    /**
     * Constructs a new <code>Feed</code>.
     * @alias module:model/Feed
     */
    constructor() { 
        
        Feed.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>Feed</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Feed} obj Optional instance to populate.
     * @return {module:model/Feed} The populated <code>Feed</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Feed();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('is_public')) {
                obj['is_public'] = ApiClient.convertToType(data['is_public'], 'Boolean');
            }
            if (data.hasOwnProperty('is_suspended')) {
                obj['is_suspended'] = ApiClient.convertToType(data['is_suspended'], 'Boolean');
            }
            if (data.hasOwnProperty('suspension_reason')) {
                obj['suspension_reason'] = ApiClient.convertToType(data['suspension_reason'], 'String');
            }
            if (data.hasOwnProperty('rss_url')) {
                obj['rss_url'] = ApiClient.convertToType(data['rss_url'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('targets')) {
                obj['targets'] = ApiClient.convertToType(data['targets'], [Target]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Feed</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Feed</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['suspension_reason'] && !(typeof data['suspension_reason'] === 'string' || data['suspension_reason'] instanceof String)) {
            throw new Error("Expected the field `suspension_reason` to be a primitive type in the JSON string but got " + data['suspension_reason']);
        }
        // ensure the json data is a string
        if (data['rss_url'] && !(typeof data['rss_url'] === 'string' || data['rss_url'] instanceof String)) {
            throw new Error("Expected the field `rss_url` to be a primitive type in the JSON string but got " + data['rss_url']);
        }
        if (data['targets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['targets'])) {
                throw new Error("Expected the field `targets` to be an array in the JSON data but got " + data['targets']);
            }
            // validate the optional field `targets` (array)
            for (const item of data['targets']) {
                Target.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Unique feed identifier
 * @member {String} id
 */
Feed.prototype['id'] = undefined;

/**
 * Feed display name
 * @member {String} name
 */
Feed.prototype['name'] = undefined;

/**
 * Whether the RSS feed is publicly accessible without a token
 * @member {Boolean} is_public
 */
Feed.prototype['is_public'] = undefined;

/**
 * Whether the feed is currently suspended (e.g. due to insufficient credits)
 * @member {Boolean} is_suspended
 */
Feed.prototype['is_suspended'] = undefined;

/**
 * Reason for suspension, if suspended
 * @member {module:model/Feed.SuspensionReasonEnum} suspension_reason
 */
Feed.prototype['suspension_reason'] = undefined;

/**
 * URL to the RSS feed. Includes a token query parameter for private feeds.
 * @member {String} rss_url
 */
Feed.prototype['rss_url'] = undefined;

/**
 * When the feed was created
 * @member {Date} created_at
 */
Feed.prototype['created_at'] = undefined;

/**
 * List of monitored targets in this feed
 * @member {Array.<module:model/Target>} targets
 */
Feed.prototype['targets'] = undefined;





/**
 * Allowed values for the <code>suspension_reason</code> property.
 * @enum {String}
 * @readonly
 */
Feed['SuspensionReasonEnum'] = {

    /**
     * value: "insufficient_credits"
     * @const
     */
    "insufficient_credits": "insufficient_credits",

    /**
     * value: "manual"
     * @const
     */
    "manual": "manual"
};



export default Feed;

